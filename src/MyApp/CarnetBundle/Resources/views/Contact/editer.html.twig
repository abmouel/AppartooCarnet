<!DOCTYPE html>
<html>
    <body>
<h1>Ajouter un contact</h1>

{% if message %}
<p>{{ message }}</p>
{% endif %}

<form action="" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" />
</form>
<p><a href="{{ path('myapp_contact_lister') }}">Retour à la liste des contacts</a></p>
    </body>
</html>